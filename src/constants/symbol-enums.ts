export enum SymbolType {
  SPOT = 1,
  FUTURE,
  DERIVATIVE,
}

export enum SymbolValue {
  BTCUSD = 1,
  USDUSDT,
  FLYUSDT,
  BTCUSDT,
}

export enum AvailableEquity {
  BTC = 1,
  USDT,
  ETH,
  USD,
  FLY,
}
